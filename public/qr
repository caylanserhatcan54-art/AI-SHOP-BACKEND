import QRCode from "qrcode";
import fs from "fs";

export async function generateShopQR(shopId: string) {
  const link = `https://ai-shop-panel.vercel.app/shop/${shopId}`;

  const filePath = `qr/${shopId}.png`;

  await QRCode.toFile(filePath, link);

  return filePath;
}
